# Core
pyodbc>=5.0.0
requests>=2.31.0
APScheduler>=3.10.4
Flask>=3.0.0
python-dateutil>=2.8.2

# Data parsing
pandas>=2.0.0
numpy>=1.24.0

# Math (Black-Scholes via SciPy normal CDF)
scipy>=1.11.0

# Timezone
pytz>=2023.3

# Email
# (uses stdlib smtplib + email — no extra package needed)

# Dev / testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
responses>=0.23.0
freezegun>=1.2.0
